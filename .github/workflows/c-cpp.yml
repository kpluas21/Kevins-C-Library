name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make DynamicArrayTest
      run: cd Data_Structures/DynamicArray/ && make -f DynamicArrayTestmake.mk
    - name: make test
      run: make -f DynamicArrayTestmake.mk test
