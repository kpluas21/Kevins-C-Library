name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: What directory is this? Where the h*ck is my makefile?
      run: ls
    - name: make DynamicArray
      run: make -f Data_Structure/DynamicArray/DynamicArrayTestmake.mk
    - name: make check
      run: make check
    - name: make distcheck
      run: make distcheck
